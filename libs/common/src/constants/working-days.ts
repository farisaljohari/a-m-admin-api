import { DaysEnum } from './days.enum';

export enum WorkingDays {
  Sun = DaysEnum.SUN,
  Mon = DaysEnum.MON,
  Tue = DaysEnum.TUE,
  Wed = DaysEnum.WED,
  Thu = DaysEnum.THU,
  Fri = DaysEnum.FRI,
  Sat = DaysEnum.SAT,
}
